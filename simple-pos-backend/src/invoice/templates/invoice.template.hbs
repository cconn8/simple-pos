<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Invoice</title>
  <style>
    body { font-family: sans-serif; margin: 2rem; }
    table { width: 100%; border-collapse: collapse; margin: 1rem 0; }
    th, td { padding: 0.5rem; text-align: left; border-bottom: 1px solid #ccc; font-size: small;}
    th { font-size: medium; background-color: #f5f5f5; }
    h2 { margin: 1rem 0 0.5rem; }
    .section-title { font-style: italic; font-weight: bold; margin-top: 2rem; padding-top: 2rem; padding-bottom: 1rem;}
    .header, .company-details, .client-dates, footer { display: flex; justify-content: space-between; }
    footer ul { display: flex; list-style: none; padding: 0; margin: 1rem 0; gap: 1rem; }
  </style>
</head>
<body>
  <!-- Header -->
  <div class="header">
    <div style="text-align: right;">
      <img src="/offd-logo.png" alt="logo" width="150" />
    </div>
  </div>

  <!-- Company Details -->
  <div class="company-details">
    <div style="margin-left: auto; text-align: right;">
      <h2>O'Flaherty Funeral Directors</h2>
      <p>Munster Avenue, Galway</p>
    </div>
  </div>

  <!-- Client + Dates -->
  <div class="client-dates" style="margin-top: 1rem;">
    <div>
      <p>C/O Representatives of the late {{data.deceasedName}}</p>
      <p>{{data.clientName}}</p>
      <p>{{data.clientAddress}}</p>
    </div>
    <div style="text-align: right;">
      <p><strong>Invoice Number:</strong> {{data.invoiceNumber}}</p>
      <p><strong>Invoice Date:</strong> {{data.invoiceDate}}</p>
      <p><strong>Due Date:</strong> {{data.dueDate}}</p>
    </div>
  </div>

  <table>
    <thead>
      <tr>
        <th>Item</th>
        <th>Unit Price</th>
        <th>Total</th>
      </tr>
    </thead>
    <tbody>
      <!-- Services Section -->
      <tr>
        <td colspan="3" class="section-title" style="border: none;">Our Services</td>
      </tr>
      {{#each services}}
      <tr>
        <td>
          <strong>{{name}}</strong><br />
          {{description}}
        </td>
        <td>{{price}}</td>
        <td>{{price}}</td>
      </tr>
      {{/each}}

      <!-- Products Section -->
      <tr>
        <td colspan="3" class="section-title" style="border: none;">Selection of Funeral Items</td>
      </tr>
      {{#each products}}
      <tr>
        <td>
          <strong>{{name}}</strong><br />
          {{description}}
        </td>
        <td>{{price}}</td>
        <td>{{price}}</td>
      </tr>
      {{/each}}

      <!-- Disbursements Section -->
      <tr>
        <td colspan="3" class="section-title" style="border: none;">External Payments on the client's behalf</td>
      </tr>
      {{#each disbursements}}
      <tr>
        <td>
          <strong>{{name}}</strong><br />
          {{description}}
        </td>
        <td>{{price}}</td>
        <td>{{price}}</td>
      </tr>
      {{/each}}
    </tbody>
  </table>

  <!-- Footer -->
  <footer>
    <ul>
      <li>O'Flaherty Funeral Directors Ltd.</li>
      <li>accounts@oflahertyfunerals.ie</li>
      <li>091 582349</li>
      <li>Bank of Ireland, Eyre Sq. Galway</li>
      <li>90384026374873</li>
    </ul>
  </footer>
</body>
</html>